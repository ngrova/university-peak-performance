# Code Patterns

Canonical patterns for common code structures. Follow these exactly — deviations get flagged by the review council.

## Server Action Structure

Pattern: getServerClient() → getUser() → validate → mutate → return { error: string | null }

Canonical example: (reference file path once established)

## Form Handling

Standard form state pattern:
(documented once the project's first form is built)

## Error Handling

Standard error return shape:
(documented once established)

## Auth Checks

Layers and patterns:
(documented once auth is implemented)

## Shared Utilities

Functions that already exist — import, don't reimplement:

| Function | Location | Purpose |
|----------|----------|---------|
| (populated as utilities are created) |

## Anti-Patterns

| Don't do this | Do this instead | Why |
|---------------|-----------------|-----|
| (populated as patterns are established) |
