# Design Tokens

Source of truth for all atomic design values. Check here before hardcoding any color, shadow, radius, or spacing value. If the value you need isn't here, add it — don't hardcode it inline.

## Colors (from tailwind.config.ts)

| Token | Value | Tailwind Class | Usage |
|-------|-------|---------------|-------|
| (populated as the project's color system is established) |

## Opacity Conventions

| Value | Purpose | Example |
|-------|---------|---------|
| (populated as opacity patterns emerge) |

## Shadows

| Name | Value | Usage |
|------|-------|-------|
| (populated as shadow patterns emerge) |

## Border Radius

| Name | Value | Tailwind Class | Usage |
|------|-------|---------------|-------|
| (populated as radius patterns emerge) |

## Anti-Patterns

These values exist as named tokens. Never hardcode them:

| Never use | Use instead | Why |
|-----------|-------------|-----|
| (populated as tokens are established) |
