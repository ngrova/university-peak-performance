[build]
  command = "npm install -g pnpm && pnpm install && pnpm --filter @upp/thriving build"
  publish = "apps/thriving/.next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
