import React from 'react';
import TodayContent from '@/components/TodayContent';

/** Today screen — the daily driver showing One Thing, queue, and overdue */
export default function TodayPage(): React.JSX.Element {
  return <TodayContent />;
}
