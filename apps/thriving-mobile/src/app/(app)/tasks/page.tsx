import React from 'react';
import TasksContent from '@/components/TasksContent';

/** Tasks screen — full task inventory with search, filter, and grouping */
export default function TasksPage(): React.JSX.Element {
  return <TasksContent />;
}
